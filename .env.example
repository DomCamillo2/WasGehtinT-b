# Canonical app lives in web/ — copy env vars from:
#   web/.env.example
#
# Root scripts (create-admin-user, sync-external-events-to-supabase, …)
# also read NEXT_PUBLIC_SUPABASE_URL / SUPABASE_* from the environment.
